apiVersion: v1
kind: Service
metadata:
  name: server
  labels:
    app: chat-crdt-server
spec:
  selector:
    app: chat-crdt-server
  ports:
    - name: http
      port: 3001
      targetPort: http
